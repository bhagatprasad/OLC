<!-- Modal -->
<div class="modal fade" id="deleteQueueConfiguration" tabindex="-1" aria-labelledby="queueConfigurationModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="queueConfigurationModalLabel">Are you sure you want to delete this Queue Configuration?</h5>
                <button type="button" class="btn-close btn-view-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>

            <div class="modal-body" style="padding:0">
                <div class="queue-configuration-card">
                    <i class="fas fa-cogs chip-icon"></i>
                    <form id="queueConfigurationForm">
                        <div class="mb-3">
                            <label for="DeleteKey" class="form-label">
                                <i class="fas fa-list-alt"></i> Key
                            </label>
                            <input type="text" class="form-control" id="DeleteKey" placeholder="Savings, Current, etc." maxlength="50" readonly>
                        </div>

                        <div class="mb-3">
                            <label for="DeleteValue" class="form-label">
                                <i class="fas fa-barcode"></i> Value
                            </label>
                            <input type="text" class="form-control" id="DeleteValue" placeholder="e.g., SAV01" maxlength="20" readonly>
                        </div>

                        <div class="mb-3">
                            <label for="DeleteDataType" class="form-label">
                                <i class="fas fa-barcode"></i> DataType
                            </label>
                            <input type="text" class="form-control" id="DeleteDataType" placeholder="e.g., SAV01" maxlength="20" readonly>
                        </div>

                        <div class="mb-3">
                            <label for="DeleteDescription" class="form-label">
                                <i class="fas fa-barcode"></i> Description
                            </label>
                            <input type="text" class="form-control" id="DeleteDescription" placeholder="e.g., SAV01" maxlength="20" readonly>
                        </div>

                        <div class="mb-3">
                            <label for="DeleteStatus" class="form-label">
                                <i class="fas fa-toggle-on"></i> Status
                            </label>
                            <select class="form-select" id="DeleteStatus" disabled>
                                <option value="true">Active</option>
                                <option value="false">Inactive</option>
                            </select>
                        </div>
                    </form>
                </div>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary btn-view-type-close" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary" id="deleteQueueConfigurationBtn">Delete</button>
            </div>
        </div>
    </div>
</div>

<style>
    .queue-configuration-card {
        background: linear-gradient(135deg, #00b09b 0%, #96c93d 100%);
        border-radius: 15px;
        padding: 20px;
        color: white;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        position: relative;
        overflow: hidden;
    }

        .queue-configuration-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="40" fill="rgba(255,255,255,0.1)"/></svg>') no-repeat center;
            background-size: cover;
            opacity: 0.1;
        }

        .queue-configuration-card .form-control {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            border-radius: 5px;
        }

            .queue-configuration-card .form-control::placeholder {
                color: rgba(255, 255, 255, 0.7);
            }

        .queue-configuration-card .form-select {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            border-radius: 5px;
        }

    .queue-configuration-card .form-select option {
                background: #00b09b;
                color: white;
            }

    .chip-icon {
        position: absolute;
        top: 20px;
        right: 20px;
        font-size: 24px;
        color: gold;
    }
</style>



